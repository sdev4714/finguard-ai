services:
  - type: web
    name: finguard-ai
    runtime: python
    region: singapore
    plan: free
    buildCommand: bash build.sh
    startCommand: bash start.sh
    envVars:
      - key: DEBUG
        value: false