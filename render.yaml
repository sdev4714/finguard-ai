services:
  - type: web
    name: finguard-ai
    runtime: python
    region: singapore
    plan: free
    buildCommand: bash build.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEBUG
        value: false